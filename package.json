{
  "name": "plotive",
  "version": "0.3.0",
  "keywords": [
    "plotting",
    "visualization",
    "charts",
    "graphs"
  ],
  "author": "Rémi Thebault",
  "description": "A library for plotting data.",
  "repository": {
    "type": "git",
    "url": "https://github.com/rtbo/plotive-js.git"
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build:wasm:web:dev": "wasm-pack build plotive-wasm --dev --target web --out-dir ../dist/esm/wasm && rm -f dist/esm/wasm/.gitignore dist/esm/wasm/package.json dist/esm/wasm/README.md",
    "build:wasm:node:dev": "wasm-pack build plotive-wasm --dev --target nodejs --out-dir ../dist/cjs/wasm && rm -f dist/cjs/wasm/.gitignore dist/cjs/wasm/package.json dist/cjs/wasm/README.md",
    "build:wasm:dev": "pnpm run build:wasm:web:dev && pnpm run build:wasm:node:dev",
    "build:wasm:web": "wasm-pack build plotive-wasm --target web --out-dir ../dist/esm/wasm && rm -f dist/esm/wasm/.gitignore dist/esm/wasm/package.json dist/esm/wasm/README.md",
    "build:wasm:node": "wasm-pack build plotive-wasm --target nodejs --out-dir ../dist/cjs/wasm && rm -f dist/cjs/wasm/.gitignore dist/cjs/wasm/package.json dist/cjs/wasm/README.md",
    "build:wasm": "pnpm run build:wasm:web && pnpm run build:wasm:node",
    "build:ts": "tsup",
    "build:dev": "pnpm run clean && pnpm run build:wasm:dev && pnpm run build:ts",
    "build": "pnpm run clean && pnpm run build:wasm && pnpm run build:ts",
    "prepublishOnly": "pnpm run build",
    "pack:check": "pnpm run build && npm pack --dry-run",
    "dev": "vite",
    "examples:install": "pnpm --dir examples install",
    "examples:dev": "pnpm run build:dev && pnpm run examples:install && pnpm --dir examples run dev"
  },
  "main": "./dist/cjs/index.cjs",
  "module": "./dist/esm/index.mjs",
  "exports": {
    ".": {
      "types": "./dist/esm/index.d.mts",
      "node": {
        "import": "./dist/node-esm/index.mjs",
        "require": "./dist/cjs/index.cjs"
      },
      "browser": "./dist/esm/index.mjs",
      "import": "./dist/esm/index.mjs",
      "require": "./dist/cjs/index.cjs",
      "default": "./dist/esm/index.mjs"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/node": "^25.8.0",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
