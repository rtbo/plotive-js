{
  "name": "plotive-js",
  "version": "0.1.0",
  "type": "module",
  "keywords": [],
  "author": "",
  "description": "",
  "scripts": {
    "build:wasm": "wasm-pack build plotive-wasm --target web",
    "build:ts": "tsc",
    "build": "pnpm run build:wasm && pnpm run build:ts",
    "dev": "vite",
    "example:install": "pnpm --dir examples/sine install",
    "example:dev": "pnpm run build && pnpm run example:install && pnpm --dir examples/sine run dev",
    "example:build": "pnpm run build && pnpm run example:install && pnpm --dir examples/sine run build"
  },
  "main": "src/index.ts",
  "dependencies": {
    "plotive-wasm": "file:./plotive-wasm/pkg"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}

